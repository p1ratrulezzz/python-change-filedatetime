# python-change-filedatetime

Example of usage winapi function SetFileTime and GetFileTime in python.

Edit process.py to use functions

```bash
pip install -r requirements.txt
python3 src/process.py
```
